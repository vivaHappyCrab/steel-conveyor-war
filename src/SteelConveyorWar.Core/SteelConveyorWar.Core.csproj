<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
  </PropertyGroup>

  <!-- Test-only friends: god-mode / setup helpers stay internal to Core, exposed only to the test
       assemblies that drive them. Do NOT add the runtime Sfml/Client assemblies without review. -->
  <ItemGroup>
    <InternalsVisibleTo Include="SteelConveyorWar.Core.Tests" />
    <InternalsVisibleTo Include="SteelConveyorWar.Sfml.Tests" />
    <InternalsVisibleTo Include="SteelConveyorWar.Benchmarks" />
  </ItemGroup>

</Project>
