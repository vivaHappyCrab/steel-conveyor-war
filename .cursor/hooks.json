{
  "version": 1,
  "hooks": {
    "beforeShellExecution": [
      {
        "type": "prompt",
        "prompt": "Steel Conveyor War git guard: DENY force-push and DENY direct push/merge into protected branches main or develop UNLESS this is clearly initial bootstrap/admin setup requested by the owner. Allow feature-branch git operations, builds, tests, smoke runs, and gh repo/issue/pr create (not merge). Hook input: $ARGUMENTS",
        "matcher": "git\\s+(push|merge)|gh\\s+pr\\s+merge",
        "timeout": 20,
        "failClosed": false
      }
    ]
  }
}
